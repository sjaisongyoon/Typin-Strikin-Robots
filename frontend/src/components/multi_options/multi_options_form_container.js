import {connect} from 'react-router-dom';
import MultiOptionsForm from './multi_options_form';

const mapStateToProps = (state) => {

}

const mapDispatchToProps = (dispatch) => {

}

export default connect(
    mapStateToProps,
    mapDispatchToProps
)(MultiOptionsForm)