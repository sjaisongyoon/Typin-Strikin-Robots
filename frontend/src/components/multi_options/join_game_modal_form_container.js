import {connect} from 'react-router-dom';
import JoinGameModalForm from './join_game_modal_form';

const mapStateToProps = (state) => {

}

const mapDispatchToProps = (dispatch) => {

}

export default connect(
    mapStateToProps,
    mapDispatchToProps
)(JoinGameModalForm)